#spring:
#  application:
#    name: order
#  jpa:
#    database: oracle
#    show-sql: true
#  datasource:
#    driver-class-name: oracle.jdbc.OracleDriver
#    url: jdbc:oracle:thin:@192.168.0.230:1521:KWW
#    username: testdatabase
#    password: sa
    
# 访问：      /{lable}/{name}-{profiles}.yml
# name 服务名
# profiles 环境名
# lable 在git上对应的是分支，默认是master分支，可以不写

# 访问如：    ip+端口/order-a.yml     ip+端口/order-b.yml          "-"后面可以随意写，但是如果写了dev或test，则会取得该文件和order-dev.yml或者order-test.yml的并集的内容
